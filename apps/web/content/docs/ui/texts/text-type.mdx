---
title: Typewriter
description: Text animation that types out a text, one letter at a time.
---

<References>
<Reference label="Fancy Components" link="https://www.fancycomponents.dev/docs/components/text/typewriter" />
</References>

<ComponentCollapse
  name="text-type-demo"
  hasReTrigger
  classNameComponentContainer="min-h-[100px]"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<ComponentCli name="text-type"/>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

<ComponentCli name="text-type"/>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="text-type" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Props

<AutoTypeTable path="./content/docs/ui/props.ts" name="TextType" />

{/* <TypeTable
  type={{
    text: {
      description:
        'The text or array of texts to be displayed and animated.',
      type: 'string | string[]',
      default: '-',
    },
    speed: {
      description:
        'Speed of typing animation in milliseconds.',
      type: 'number',
      default: '50',
    },
    initialDelay: {
      description:
        'Delay before typing starts in milliseconds.',
      type: 'number',
      default: 0,
    },
    waitTime: {
      description:
        'Time to wait before deleting text in milliseconds',
      type: 'number',
      default: '2000',
    },
    loop: {
      description:
        'Whether to loop through the texts.',
      type: 'boolean',
      default: 'true',
    },
    deleteSpeed: {
      description:
        'Speed of delete animation in milliseconds.',
      type: 'number',
      default: '30',
    },
    className: {
      description:
        'Additional CSS classes for styling.',
      type: 'string',
      default: 'undefined',
    },
    showCursor: {
      description:
        'Whether to show the cursor.',
      type: 'boolean',
      default: 'true',
    },
    hideCursorOnType: {
      description:
        'Whether to hide the cursor when typing.',
      type: 'boolean',
      default: 'false',
    },
    cursorChar: {
      description:
        'Character or node to use as cursor.',
      type: 'string | React.ReactNode',
      default: '|',
    },
    cursorClassName: {
      description:
        'Additional CSS classes for cursor styling.',
      type: 'string',
      default: '"ml-0.5"',
    },
  }}
/> */}

