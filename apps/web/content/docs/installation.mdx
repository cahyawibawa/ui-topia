---
title: Installation
description: A guide to setting up your project with the necessary dependencies and structure.
icon: PackagePlus
---

This project is built with Next.js and leverages TypeScript and Tailwind CSS for all its components. Some components also require [shadcn/ui](https://ui.shadcn.com/docs/installation) and Framer Motion. While we recommend TypeScript for optimal development experience, JavaScript is also supported.

<Steps>
<Step>
### Install Tailwind CSS

Components are styled using Tailwind CSS. Follow the official [installation guide](https://tailwindcss.com/docs/installation) to add it to your project.
</Step>

<Step>

### Install Framer Motion

<InstallTabs items={['pnpm', 'bun']}>

```bash tab="pnpm"
pnpm add framer-motion

```
```bash tab="bun"
bunx add framer-motion

```

</InstallTabs>

</Step>


<Step>

### Add cn helper

Add a `cn` helper to make it easier to conditionally add Tailwind CSS classes.

```ts title="lib/utils.ts"
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

</Step>
<Step>
### App structure
Here's how I structure my Next.js apps. You can use this as a reference:

      <Files>
        <Folder name="components" defaultOpen>
        <Folder name="ui-topia" defaultOpen>
             <File name="accordion.tsx" />
        </Folder>
        </Folder>
          <Folder name="lib" defaultOpen>
             <File name="utils.ts" />
        </Folder>

      </Files>

You're all set! You can now start enjoying the components.

</Step>


</Steps>
