---
title: Text Type
description: Text animation that types out a text, one letter at a time.
---

<References>
<Reference label="Fancy Components" link="https://www.fancycomponents.dev/docs/components/text/typewriter" />
</References>

### Example

<ComponentCollapse
  name="text-type-demo"
  hasReTrigger
/>

## Installation

<Steps>
<Step>Copy and paste the following code into your project.</Step>
<ComponentBase name="text-type-base" />
</Steps>

## Props

<TypeTable
  type={{
    text: {
      description:
        'The text or array of texts to be displayed and animated.',
      type: 'string | string[]',
      default: '-',
    },
    speed: {
      description:
        'Speed of typing animation in milliseconds.',
      type: 'number',
      default: '50',
    },
    initialDelay: {
      description:
        'Delay before typing starts in milliseconds.',
      type: 'number',
      default: 0,
    },
    waitTime: {
      description:
        'Time to wait before deleting text in milliseconds',
      type: 'number',
      default: '2000',
    },
    loop: {
      description:
        'Whether to loop through the texts.',
      type: 'boolean',
      default: 'true',
    },
    deleteSpeed: {
      description:
        'Speed of delete animation in milliseconds.',
      type: 'number',
      default: '30',
    },
    className: {
      description:
        'Additional CSS classes for styling.',
      type: 'string',
      default: 'undefined',
    },
    showCursor: {
      description:
        'Whether to show the cursor.',
      type: 'boolean',
      default: 'true',
    },
    hideCursorOnType: {
      description:
        'Whether to hide the cursor when typing.',
      type: 'boolean',
      default: 'false',
    },
    cursorChar: {
      description:
        'Character or node to use as cursor.',
      type: 'string | React.ReactNode',
      default: '|',
    },
    cursorClassName: {
      description:
        'Additional CSS classes for cursor styling.',
      type: 'string',
      default: '"ml-0.5"',
    },
  }}
/>

